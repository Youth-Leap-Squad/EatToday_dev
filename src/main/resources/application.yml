server:
  port: 0

spring:
  security:
    user:
      name: user
      password: 1234
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/mydb
    username: swcamp
    password: swcamp
  jpa:
    hibernate:
     ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        '[format_sql]': true    # jpa 기본 설정

logging:
  level:
    com:
      eat:
        today:
          member:
            query: debug  # 로그찍기


