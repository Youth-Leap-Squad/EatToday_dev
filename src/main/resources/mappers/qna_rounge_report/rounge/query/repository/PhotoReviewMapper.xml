<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.eat.today.qna_rounge_report.rounge.query.repository.PhotoReviewMapper">

    <resultMap id="photoReviewMap" type="com.eat.today.qna_rounge_report.rounge.query.dto.PhotoReviewDTO">
        <result column="board_no"       property="boardNo"/>
        <result column="member_no"      property="memberNo"/>
        <result column="review_title"   property="reviewTitle"/>
        <result column="review_date"    property="reviewDate"/>
        <result column="review_content" property="reviewContent"/>
        <result column="review_like"    property="reviewLike"/>
        <association property="member" resultMap="memberMap"/>
    </resultMap>

    <resultMap id="memberMap" type="com.eat.today.member.query.dto.RequestNameDTO">
        <result column="member_id" property="memberName"/>
    </resultMap>

    <select id="selectAllOrderByDateDescPaged" resultMap="photoReviewMap">
        SELECT
            pr.board_no
             , pr.member_no
             , m.member_id
             , pr.review_title
             , pr.review_date
             , pr.review_content
             , pr.review_like
        FROM photo_review pr
                 JOIN member m ON m.member_no = pr.member_no
        ORDER BY pr.review_date DESC
            LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAll" resultType="long">
        SELECT COUNT(*) FROM photo_review
    </select>

    <select id="selectAllOrderByLikeDescPaged" resultMap="photoReviewMap">
        SELECT
            pr.board_no
             , pr.member_no
             , m.member_id
             , pr.review_title
             , pr.review_date
             , pr.review_content
             , pr.review_like
        FROM photo_review pr
                 JOIN member m ON m.member_no = pr.member_no
        ORDER BY pr.review_like DESC
            LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countAllByLike" resultType="long">
        SELECT COUNT(*) FROM photo_review
    </select>

    <select id="searchByKeywordPaged" resultMap="photoReviewMap">
        SELECT
        pr.board_no
        , pr.member_no
        , m.member_id
        , pr.review_title
        , pr.review_date
        , pr.review_content
        , pr.review_like
        FROM photo_review pr
        JOIN member m ON m.member_no = pr.member_no
        <where>
            <if test="keyword != null and keyword != ''">
                (pr.review_title   LIKE CONCAT('%', #{keyword}, '%')
                OR pr.review_content LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="keyword == null or keyword == ''">
                1 = 0
            </if>
        </where>
        ORDER BY pr.review_date DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByKeyword" resultType="long">
        SELECT COUNT(*)
        FROM photo_review pr
        <where>
            <if test="keyword != null and keyword != ''">
                (pr.review_title   LIKE CONCAT('%', #{keyword}, '%')
                OR pr.review_content LIKE CONCAT('%', #{keyword}, '%'))
            </if>
            <if test="keyword == null or keyword == ''">
                1 = 0
            </if>
        </where>
    </select>

    <select id="selectByAlcoholNoPaged" resultMap="photoReviewMap">
        SELECT
            pr.board_no,
            pr.member_no,
            m.member_id,
            pr.review_title,
            pr.review_date,
            pr.review_content,
            pr.review_like
        FROM photo_review pr
                 JOIN food_post fp ON fp.board_no = pr.board_no
                 JOIN member m     ON m.member_no = pr.member_no
        WHERE fp.alcohol_no = #{alcoholNo}
        ORDER BY pr.review_date DESC
            LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="selectByAlcoholNoLikeDescPaged" resultMap="photoReviewMap">
        SELECT
            pr.board_no,
            pr.member_no,
            m.member_id,
            pr.review_title,
            pr.review_date,
            pr.review_content,
            pr.review_like
        FROM photo_review pr
                 JOIN food_post fp ON fp.board_no = pr.board_no
                 JOIN member m     ON m.member_no = pr.member_no
        WHERE fp.alcohol_no = #{alcoholNo}
        ORDER BY pr.review_like DESC, pr.review_date DESC
            LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByAlcoholNo" resultType="long">
        SELECT COUNT(*)
        FROM photo_review pr
                 JOIN food_post fp ON fp.board_no = pr.board_no
        WHERE fp.alcohol_no = #{alcoholNo}
    </select>

    <select id="selectByMemberNoPaged" resultMap="photoReviewMap">
        SELECT
            pr.board_no
             , pr.member_no
             , m.member_id
             , pr.review_title
             , pr.review_date
             , pr.review_content
             , pr.review_like
        FROM photo_review pr
                 JOIN member m ON m.member_no = pr.member_no
        WHERE pr.member_no = #{memberNo}
        ORDER BY pr.review_date DESC
            LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countByMemberNo" resultType="long">
        SELECT COUNT(*) FROM photo_review WHERE member_no = #{memberNo}
    </select>

</mapper>